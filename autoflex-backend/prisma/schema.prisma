generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // url      = env("DATABASE_URL")
}

model Product {
  id        Int      @id @default(autoincrement())
  code      String   @unique
  name      String
  price     Decimal  @db.Decimal(10, 2)

  materials ProductRawMaterial[]
  simulationItems ProductionSimulationItem[]  // 👈 ADICIONE ISSO

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  productions Production[]
}

model RawMaterial {
  id            Int      @id @default(autoincrement())
  code          String   @unique
  name          String
  stockQuantity Decimal  @db.Decimal(10, 2)

  products      ProductRawMaterial[]

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  productionMaterials ProductionMaterial[]
}

model ProductRawMaterial {
  id               Int     @id @default(autoincrement())

  productId        Int
  rawMaterialId    Int

  requiredQuantity Decimal @db.Decimal(10, 2)

  product          Product     @relation(fields: [productId], references: [id], onDelete: Cascade)
  rawMaterial      RawMaterial @relation(fields: [rawMaterialId], references: [id], onDelete: Cascade)

  @@unique([productId, rawMaterialId])
}

model ProductionSimulation {
  id        Int      @id @default(autoincrement())
  totalValue Float
  createdAt DateTime @default(now())

  items ProductionSimulationItem[]
}

model ProductionSimulationItem {
  id            Int   @id @default(autoincrement())
  quantity      Int
  unitPrice     Float
  total         Float

  productId     Int
  simulationId  Int

  product       Product @relation(fields: [productId], references: [id])
  simulation    ProductionSimulation @relation(fields: [simulationId], references: [id])
}

model Production {
  id        Int      @id @default(autoincrement())

  productId Int
  product   Product  @relation(fields: [productId], references: [id])

  quantityProduced Int
  createdAt DateTime @default(now())

  materials ProductionMaterial[]
}

model ProductionMaterial {
  id            Int     @id @default(autoincrement())

  productionId  Int
  rawMaterialId Int

  quantityUsed  Decimal @db.Decimal(10, 2)

  production    Production  @relation(fields: [productionId], references: [id], onDelete: Cascade)
  rawMaterial   RawMaterial @relation(fields: [rawMaterialId], references: [id], onDelete: Cascade)
}